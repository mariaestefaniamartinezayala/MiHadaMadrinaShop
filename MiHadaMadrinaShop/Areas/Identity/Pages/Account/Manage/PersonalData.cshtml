@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Informaci&oacute;n personal";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Su cuenta contiene datos personales que nos ha proporcionado. Esta p&aacute;gina le permite descargar o eliminar esos datos.</p>
        <p>
            <strong>Eliminar estos datos eliminar&aacute; permanentemente su cuenta y no se podr&aacute; recuperar.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class="btn btn-primary" type="submit">Descargar</button>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">Eliminar</a>
        </form>
       
       
          
       
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
